from ztools import *

# 显示 timeout 版本号
timeout().Version(isShow = True)

# 显示 progressbar 版本号
progressbar().Version(isShow = True)

# 显示 plot 版本号
plot("test").Version(isShow = True)

# 显示 filebase 版本号
filebase().Version(isShow = True)

# 显示 xls 版本号
xls().Version(isShow = True)

# 显示 MySQL 版本号
MySQL().Version(isShow = True)

input()
